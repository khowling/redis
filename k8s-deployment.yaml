apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-stream-app
  labels:
    app: redis-stream-app
spec:
  replicas: 5
  selector:
    matchLabels:
      app: redis-stream-app
  template:
    metadata:
      labels:
        app: redis-stream-app
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: redis-stream-app-sa
      containers:
      - name: redis-client
        image: kharc.azurecr.io/redis-client:0.9-arm64-with-health # Optimized for AKS performance + health checks
        ports:
        - containerPort: 8080
        env:
        # Load common configuration from ConfigMap
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: redis-stream-app-config
              key: ENVIRONMENT
        - name: REDIS_USE_AAD
          valueFrom:
            configMapKeyRef:
              name: redis-stream-app-config
              key: REDIS_USE_AAD
        - name: REDIS_ENDPOINT
          valueFrom:
            configMapKeyRef:
              name: redis-stream-app-config
              key: REDIS_ENDPOINT
        - name: REDIS_MAX_RETRIES
          valueFrom:
            configMapKeyRef:
              name: redis-stream-app-config
              key: REDIS_MAX_RETRIES
        - name: REDIS_POOL_SIZE
          valueFrom:
            configMapKeyRef:
              name: redis-stream-app-config
              key: REDIS_POOL_SIZE
        - name: REDIS_DIAL_TIMEOUT
          valueFrom:
            configMapKeyRef:
              name: redis-stream-app-config
              key: REDIS_DIAL_TIMEOUT
        
        # Azure Managed Identity Configuration (secrets, keep as env vars)
        - name: AZURE_CLIENT_ID
          value: "7303e014-4009-403f-b170-83ef57374e21" # User Managed Identity Object ID (required for Azure Redis auth)
        - name: AZURE_TENANT_ID
          value: "MngEnvMCAP134668.onmicrosoft.com" # Replace with your Azure Tenant ID
        
        # Managed Identity Object ID for Redis ACL (used as username in Redis connection)
        - name: REDIS_USERNAME
          value: "7303e014-4009-403f-b170-83ef57374e21" # Object ID of your managed identity

        resources:
          requests:
            memory: "128Mi"
            cpu: "1000m"
        
        # Health checks
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        
        # Security context
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          runAsGroup: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        
        # Volume mounts for workload identity
        volumeMounts:
        - name: azure-identity-token
          mountPath: /var/run/secrets/azure/tokens
          readOnly: true
      
      volumes:
      - name: azure-identity-token
        projected:
          sources:
          - serviceAccountToken:
              path: azure-identity-token
              expirationSeconds: 3600
              audience: api://AzureADTokenExchange

---
apiVersion: v1
kind: Service
metadata:
  name: redis-stream-app-service
  labels:
    app: redis-stream-app
spec:
  selector:
    app: redis-stream-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: redis-stream-app-sa
  annotations:
    azure.workload.identity/client-id: "e5833ec1-dc2b-471c-a883-a9e8544b2efa" # User Managed Identity Client ID
  labels:
    azure.workload.identity/use: "true"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-stream-app-config
data:
  ENVIRONMENT: "production"
  REDIS_USE_AAD: "true"
  REDIS_MAX_RETRIES: "3"
  REDIS_POOL_SIZE: "50"
  REDIS_DIAL_TIMEOUT: "5s"
  REDIS_ENDPOINT: "khredis.westeurope.redis.azure.net:10000"
